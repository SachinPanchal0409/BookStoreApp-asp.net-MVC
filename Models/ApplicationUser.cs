using Microsoft.AspNetCore.Identity;

namespace BookStoreApp.Models
{
    public class ApplicationUser : IdentityUser
    {
        // You can add more fields if needed (e.g., FirstName, LastName)
    }
}
